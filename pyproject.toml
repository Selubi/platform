[project]
name = "platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pulumi>=3.224.0",
    "pulumi-cloudflare>=6.13.0",
]
